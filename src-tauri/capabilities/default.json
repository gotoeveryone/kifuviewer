{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for local SGF open/save.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
